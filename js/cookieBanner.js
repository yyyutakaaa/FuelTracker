// FuelTracker - Cookie Banner (Removed)

export function showCookieBanner() {
    // Cookie banner removed as we only use localStorage
    // No cookies are used in this application
    console.log('FuelTracker uses only localStorage, no cookies');
}